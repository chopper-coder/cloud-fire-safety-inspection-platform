# V5.1.4 Test Reliability Fix Edition 測試報告

## 修正結論

V5.1.3 平台本體可操作，但測試腳本仍有兩個確定錯誤：

1. 導覽成功後等待不存在的 `#page-tutor`、`#page-cases`、`#page-compare`。
2. 手機抽屜關閉時只是以 CSS 移出畫面，Playwright `is_visible()` 仍可能回傳真值，造成測試不開啟抽屜就直接點擊畫面外按鈕。

V5.1.4 已修正上述問題，並新增資料匯入、正式模擬配置與完整交卷流程的回歸測試。

## 額外發現並修正的正式功能 Bug

V5.1.3 的備份匯出會包含 `gpai_v500_enterprise` 案例研習進度，但 `validImportedValue()` 未接受該資料鍵，因此匯入時會無聲略過案例完成紀錄與答對統計。V5.1.4 已加入案例資料驗證與正規化。

同時強化：

- 測驗進度依目前題庫 ID 重新水合；題目不存在或資料長度不一致時拒絕匯入。
- 統計數值不得為負數，答對數不得大於作答數，原始得分不得大於滿分。
- 歷史最多保留 500 筆，處理紀錄與測驗 ID 均限制格式及長度。
- 錯題、收藏與題目統計只接受目前題庫存在的 ID。
- 案例完成紀錄只接受目前案例庫存在的 ID 與有效選項索引。

## 實際測試結果

執行指令：

```bash
GPAI_EMBEDDED_TEST=1 CHROMIUM_PATH=/usr/bin/chromium python tests/smoke_test.py
```

結果：

```text
PASS: V5.1.4 static integrity, responsive UI, mobile navigation, official exam, quick exam, results, and backup validation
```

本執行環境會封鎖瀏覽器對 `file://` 與 localhost 的導覽，因此測試程式使用與正式檔案相同的 HTML、CSS、題庫及 JavaScript，建立隔離式內嵌頁面執行。這不是略過瀏覽器測試；所有互動仍在實際 Chromium 中由 Playwright 點擊與驗證。

## 靜態與語法檢查

- `tests/smoke_test.py` Python 編譯：通過
- `app.js` Node.js 語法：通過
- `service-worker.js` Node.js 語法：通過
- 所有 `data/*.js` Node.js 語法：通過
- Web App Manifest JSON：通過
- HTML 引用檔案缺漏：0
- APP_VERSION：`5.1.4`
- PWA 快取：`gpai-v514-20260726`
- 重要輸入及篩選欄位無障礙標籤：通過

## 響應式與核心互動

### 1440 × 1000 桌機
- 固定側邊欄顯示正常
- 內容左側偏移正常
- 手機底部導覽隱藏
- AI 採購老師可輸入問題並產生回答
- 案例選項可作答並顯示回饋
- 法條比較可渲染

### 1024 × 900 平板
- 固定側邊欄與 12px 內距正常
- 內容偏移正常
- AI 老師、案例與比較頁切換正常

### 390 × 844 手機
- 桌面側邊欄隱藏
- 內容 `margin-left:0`
- 底部導覽顯示
- 「更多」抽屜初始為關閉
- 依 `aria-hidden=false` 與 `sheet-open` 確認開啟
- AI 老師、案例、法條比較與設定頁可正常切換
- 每次切換後抽屜確實關閉

## 正式三節模擬

- 第 1 節：78 題，270 分
- 第 2 節：69 題，240 分
- 第 3 節：78 題，270 分
- 合計：225 題，780 分
- 三節合計唯一題目：225 題
- 各節 80 分鐘：通過
- 各科是非題與選擇題數量符合 `data/scheme.js`：通過
- 各節 2 分／5 分加權總和等於節次配分：通過
- 各科來源題池容量大於或等於需求：通過
- 30 輪隨機抽題壓力測試：0 次失敗
- 作答、標記與下一題狀態寫入：通過

## 快速測驗與交卷

- 快速測驗：50 題
- 是非題：15 題
- 選擇題：35 題
- 重複題：0
- 全部答對後強制交卷：100 分
- 歷史紀錄新增：1 筆
- 累積作答與答對統計：各 50

## 備份與異常資料

- 有效測驗進度可接受：通過
- 不存在題目 ID 的進度拒絕：通過
- 案例作答次數與答對次數可還原：通過
- 負數統計歸零：通過
- 答對數限制不得超過作答數：通過
- 原始得分限制不得超過滿分：通過
- 非字串處理紀錄移除：通過

## 未宣稱的範圍

- 法規版本核對不等於 3,664 題答案逐題人工認證。
- Service Worker 的真實離線更新流程須在 HTTPS 或 localhost 環境進行，現有測試以快取清單及版本靜態檢查為主。
- 不同手機廠牌的瀏覽器 PWA 安裝提示仍可能因瀏覽器政策不同而有差異。
